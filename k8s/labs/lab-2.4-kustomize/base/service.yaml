apiVersion: v1
kind: Service
metadata:
  name: learnhub-course-kustomize
  labels:
    app: learnhub-course-kustomize
    component: course
    lab: "2.4"
spec:
  type: ClusterIP
  selector:
    app: learnhub-course-kustomize
  ports:
    - name: http
      port: 80
      targetPort: http
