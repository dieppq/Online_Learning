Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

Check rollout:

  kubectl rollout status deployment/{{ include "learnhub-course.fullname" . }} -n {{ .Release.Namespace }}

Smoke test from inside the namespace:

  kubectl run helm-course-curl --rm -i --restart=Never --image=curlimages/curl:8.10.1 -n {{ .Release.Namespace }} -- curl --fail --silent http://{{ include "learnhub-course.fullname" . }}/api/courses

Rollback example:

  helm history {{ .Release.Name }} -n {{ .Release.Namespace }}
  helm rollback {{ .Release.Name }} 1 -n {{ .Release.Namespace }}

