apiVersion: batch/v1
kind: Job
metadata:
  name: learnhub-database-secret-check
  labels:
    app.kubernetes.io/name: learnhub-database-secret-check
    app.kubernetes.io/part-of: learnhub
    app.kubernetes.io/component: config-check
spec:
  backoffLimit: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: learnhub-database-secret-check
        app.kubernetes.io/part-of: learnhub
        app.kubernetes.io/component: config-check
    spec:
      serviceAccountName: learnhub-app
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: secret-check
          image: busybox:1.36
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              set -eu
              test -n "$JWT_SECRET"
              test -n "$USER_DB_PASSWORD"
              test -n "$COURSE_DB_PASSWORD"
              test -n "$ENROLLMENT_DB_PASSWORD"
              test -n "$PAYMENT_DB_PASSWORD"
              test -n "$NOTIFICATION_DB_PASSWORD"
              echo JWT_SECRET=present
              echo service_database_secrets=present
          env:
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: learnhub-secret
                  key: JWT_SECRET
            - name: USER_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: user-service-db
                  key: POSTGRES_PASSWORD
            - name: COURSE_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: course-service-db
                  key: POSTGRES_PASSWORD
            - name: ENROLLMENT_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: enrollment-service-db
                  key: POSTGRES_PASSWORD
            - name: PAYMENT_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: payment-service-db
                  key: POSTGRES_PASSWORD
            - name: NOTIFICATION_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: notification-service-db
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 32Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
